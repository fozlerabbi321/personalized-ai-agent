"""Domain layer — pure business entities, repository protocols, and exceptions.

This layer has ZERO external dependencies (no FastAPI, no asyncpg, no LangChain).
It defines the core business rules and contracts that all other layers depend on.
"""
